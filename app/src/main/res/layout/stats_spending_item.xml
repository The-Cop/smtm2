<?xml version="1.0" encoding="utf-8"?>


<android.support.v7.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:layout_marginTop="4dp"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        app:cardCornerRadius="4dp"
        app:cardElevation="2dp">

    <android.support.constraint.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <TextView
                tools:text="30.08.2088"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/textViewDate"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                android:layout_marginLeft="4dp"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginTop="2dp"
                />

        <TextView
                tools:text="899 986.13"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/textViewAmount"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                android:layout_marginRight="4dp"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintBottom_toBottomOf="@+id/textViewDate"
                android:layout_marginBottom="0dp"/>

        <TextView
                tools:text="Electronics category is so long"
                android:ellipsize="end"
                android:layout_width="0dp"
                android:maxLines="1"
                android:layout_height="wrap_content"
                android:id="@+id/textViewCategory"
                android:layout_marginRight="8dp"
                app:layout_constraintBottom_toBottomOf="@+id/textViewAmount"
                android:layout_marginBottom="0dp"
                app:layout_constraintRight_toLeftOf="@+id/textViewAmount"
                app:layout_constraintLeft_toRightOf="@+id/textViewDate"
                android:layout_marginLeft="8dp"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                app:layout_constraintHorizontal_bias="0.0"/>

        <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:id="@+id/textViewComment"
                app:layout_constraintLeft_toLeftOf="@+id/textViewDate"
                android:layout_marginLeft="0dp"
                android:layout_marginTop="4dp"
                app:layout_constraintTop_toBottomOf="@+id/textViewDate"
                app:layout_constraintBottom_toBottomOf="parent"
                android:layout_marginBottom="2dp"
                tools:text="Long comment lets test it omg so loandles it fdsf sdf sdf sf sdf sdf sdfs"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_marginRight="4dp"
                app:layout_constraintVertical_bias="0.0"
                app:layout_constraintRight_toRightOf="parent"
                android:textStyle="italic"/>

    </android.support.constraint.ConstraintLayout>

    <!--TODO replace all dravables to vector-->
    <!--TODO replace all android:src to app:srcCompat-->
</android.support.v7.widget.CardView>

